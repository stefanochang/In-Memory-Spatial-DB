In Ubuntu terminal navigate to folder where code is saved and Do the following:
1. sudo apt-get install libboost-all-dev
2. g++ rtreeTest.cpp -L/usr/local/lib/ -lboost_filesystem
2. ./a.out

Extra instruction
Please use the files that are outside the folders. That is data-indexing-main and rtree-implementation for integration purpose. Rest folder that are inside, are for our internal purpose. 